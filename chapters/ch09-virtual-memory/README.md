# Chapter 9: Virtual Memory

Use `allocator_notes.c` as a scratchpad for allocator invariants before Malloc
Lab. It does not implement malloc; it only models block metadata.

Commands:

```sh
cc -Wall -Wextra -O0 -o allocator_notes allocator_notes.c
./allocator_notes
```
