# Chapter 8: Exceptional Control Flow

Use `process.c` to practice `fork`, `waitpid`, and exit status handling before
Shell Lab.

Commands:

```sh
cc -Wall -Wextra -O0 -o process process.c
./process
```
