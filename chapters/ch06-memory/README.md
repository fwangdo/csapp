# Chapter 6: The Memory Hierarchy

Use `cache_probe.c` to observe how access stride changes runtime. Then move to
Cache Lab for the simulator and matrix transpose work.

Commands:

```sh
cc -Wall -Wextra -O2 -o cache_probe cache_probe.c
./cache_probe
```
