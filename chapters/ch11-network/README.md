# Chapter 11: Network Programming

Use `echo.c` as a small socket placeholder before Proxy Lab. Fill in one side at
a time: address lookup, socket, bind/connect, then read/write.

Commands:

```sh
cc -Wall -Wextra -O0 -o echo echo.c
./echo
```
