# Chapter 12: Concurrent Programming

Use `counter.c` to see the difference between unsynchronized and synchronized
updates before adding concurrency to Proxy Lab.

Commands:

```sh
cc -Wall -Wextra -O0 -pthread -o counter counter.c
./counter
```
